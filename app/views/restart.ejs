<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="refresh" content="60;url=/" />
		<title>Restart</title>
	</head>
	<body>
		<div>
			The device is restarting. You will be redirected to the status page in <span id='time-left'>60</span> seconds.
		</div>
    <div>
      If you are not redirected, do not refresh this page or the device may be restarted again unintentionally.
    </div>
		<script type='text/javascript'>
			var timeLeft = 60;
			var timer = function() {
				timeLeft--;
				if (timeLeft >= 0) {
					document.getElementById('time-left').innerHTML = timeLeft;
				}
			}
			setInterval(timer, 1000);

			var xhr = new XMLHttpRequest();
			xhr.open("POST", "/restart", true);
			xhr.send();
		</script>
	</body>
</html>
